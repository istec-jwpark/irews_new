auth_info:
  desc:
    params: |
      user_id(str)
    result: |
      사용자 정보 조회
  query: |
    SELECT 
      user_nm as "userNm",
      roll_cd as "rollCd",
      user_id as "userId",
      user_pwd as "userPwd"
    FROM TB_M1_INFO_USER_BASE
    WHERE user_id = :user_id