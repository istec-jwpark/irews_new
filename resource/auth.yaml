find_user:
  desc:
    params: |
      user_id(str)
    result: |
      사용자 아이디에 대한 사용자 정보
  query: |
    SELECT USER_ID, USER_PWD, USER_NM, GROUP_SQ, SITE_SQ, USER_SQ, ROLL_CD
    FROM TB_M1_INFO_USER_BASE
    WHERE USER_ID  = :user_id